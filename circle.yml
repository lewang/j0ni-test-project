machine:
  environment:
    cyvar1: bar
    cyvar2: 123
  # node:
    # version: 0.8.12

# dependencies:
  # override:
    # - lein deps
    # - npm install npm@1.4.4
    # - npm install

test:
  post:
    - mkdir artifacts
    - echo "I am an artifact" > artifacts/artifact.txt
