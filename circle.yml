machine:
  environment:
    cyvar1: bar
    cyvar2: 123

notify:
  webhooks:
    - url: http://mal.lollyshouse.net:7777/

deployment:
  auto_push_to_upstream_development:
    branch: zonkey
    commands:
    -
