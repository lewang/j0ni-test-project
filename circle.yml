machine:
  environment:
    cyvar1: bar
    cyvar2: 123
  node:
    version: 0.12.0

# dependencies:
  # override:
    # - lein deps
    # - npm install npm@1.4.4
    # - npm install

test:
  post:
    - echo "I am an artifact" > $CIRCLE_ARTIFACTS/artifact.txt
