machine:
  environment:
    cyvar1: bar
    cyvar2: 123
  node:
    version: 0.12.0

dependencies:
  pre:
  - if [ ! -e ~/nvm/versions/v0.12.0 ]; then nvm install 0.12.0; fi

  cache_directories:
  - "~/nvm/versions/v0.12.0"

test:
  post:
    - echo "I am an artifact" > $CIRCLE_ARTIFACTS/artifact.txt
